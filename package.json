{
  "scripts": {
    "build": "tsc",
    "dev": "node build/cmd/index.js",
    "nodemon": "nodemon --exec \"npm run dev\" --ignore ./build --verbose  --ext ts ."
  },
  "devDependencies": {
    "@types/node": "^20.2.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@google-cloud/storage": "^6.10.1",
    "googleapis": "^118.0.0"
  }
}
